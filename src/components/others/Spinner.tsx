/**
 * * Simple component Spinner
 * ? Spinner or loading animation
 * @returns Simple component
 */
export const Spinner = () => (
	<div className="lds-ripple">
		<div></div>
		<div></div>
	</div>
);
